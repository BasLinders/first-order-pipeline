name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        env:
          # FOE_GITHUB_TOKEN is a PAT with repo scope, stored as a repository
          # secret. GITHUB_TOKEN cannot be used here because it is scoped to
          # this repo only and cannot access first-order-engine (private repo).
          FOE_TOKEN: ${{ secrets.FOE_GITHUB_TOKEN }}
        run: |
          pip install pytest pytest-mock
          pip install -r requirements.txt
          git config --global url."https://oauth2:${FOE_TOKEN}@github.com/".insteadOf "https://github.com/"
          pip install git+https://github.com/BasLinders/first-order-engine.git@main

      - name: Run tests
        run: pytest tests/
